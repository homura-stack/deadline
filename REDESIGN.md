# DEAD/LINE — アストラ全面改装

## 改装前の保護（2026-09-06）

コード変更前に実行した確認：

| 確認項目 | 結果 |
| --- | --- |
| リポジトリ | `.`、独立したGitリポジトリ |
| 開始branch / HEAD | `main` / `c9276a0f97a2b0d000c2c0f3ad20b20ffcaffda6` |
| staged / unstaged / 未追跡 | すべてなし |
| ignore対象 | `tests/artifacts/` の既存検証画像・JSON。作品実行に必要なファイルはすべて追跡済み |
| remote | `origin https://github.com/homura-stack/deadline.git` |
| remote実確認 | `git ls-remote` で `refs/heads/main` が上記SHAと一致 |
| 整合性 | `git fsck --full` に破損なし。既存のdangling blobが1件、削除なし |
| 復旧基準 | `backup/deadline-before-astra-20260906`、同じSHA |
| 外部の作業バックアップ | 全履歴bundle＋全作業ファイルZIP。bundle verify成功 |
| 改装branch | 上記SHAから `codex/web-contest-astra-redesign` を作成 |

この保護を完了した後にHTML・CSS・JavaScriptを読み、実装へ進みました。操作手順は [RESTORE.md](RESTORE.md) を参照。

## 作品分析

HTML、既存CSS、config / simulation / game / renderer / audioの全体と、改装前の実Chrome画面を確認しました。

| 観点 | 確認した核と改装方針 |
| --- | --- |
| 核となる遊び | 生存してゲージを溜め、世界を停止し、一筆書きの経路を実行する。全10 Wave |
| 判断 | 実弾の隙間、TARGETの順序、残り時間、最後の退避位置。後半は全敵を1回でロックする |
| 気持ちよさ | 自分で計画した一本の線が、短い溜めの後に連鎖撃破へ変わること |
| 視認性 | 元版の格子背景を抑えた計器の盤面へ。プレイヤー、敵、弾の形状差は維持する |
| 操作感 | マウス／SPACE、Z・X・C、タッチパッドの入力経路を維持。画面装飾はpointer-eventsを透過 |
| UI・HUD | 現在の状態、Wave、得点を読み取りやすい文字サイズに。充電と実行可能状態を色分け |
| タイトル・第一印象 | 中央PNGロゴから、大きな二段の文字組みと実際の遊びを表す動く経路の構図へ |
| 色彩 | 深い藍、薄い紫、翡翠色の導線、珊瑚色の敵、暖色の実弾。発火だけ金白色に切り替える |
| タイポグラフィ | Impact等のローカル表示書体、Consolas等の等幅数値、日本語UI書体。外部フォント不要 |
| 敵・危険物 | AIM三角、FAN五角、BURST円、ROTATE四角、DELAY六角を維持。敵の装甲面を追加。停止中も弾は明るく残す |
| 瞬間移動・導線 | 元のポリラインをそのまま描き、順序番号と向きの印を追加。危険区間は赤と×で区別 |
| 発火 | 金白色の走る光、局所的な発光、斬撃、放射状の破片、連続得点、完了経路の余韻 |
| 画面遷移 | 既存のタイトル離脱時間を維持し、金色の切断線へ。説明は元の同期デモと入力を維持 |
| GAME OVER / CLEAR | 全画面の文字組みと上下の切断線で演出。得点・Wave・再挑戦・TITLEを明確にする |

## 比較した3つのアートディレクション

| 案 | 遊びとの相性 | 判断 |
| --- | --- | --- |
| ネオン都市・サイバー空間 | 超高速移動は映えるが、背景と弾幕の発光が経路と競合しやすい | 不採用 |
| 白黒の図面・高コントラスト | 計画は読みやすいが、停止と発火の温度差や報酬の色を使いにくい | 不採用 |
| 時間を観測する計器・空間を切る光 | 円周目盛りが停止を表し、静かな線から金白の斬光へ変わる瞬間が遊びと一致する | 採用 |

パレット：夜 `#080c1b`、計器 `#101627`、紙白 `#eef3ef`、導線 `#a8eee7`、敵・警告 `#f28d91`、発火 `#f6cc86`。

```
TITLE:   [大きなDEAD / LINE]     [停止した弾・TARGET・一本の光]
         [短い操作の宣言]       [FREEZE → DRAW → EXECUTEの循環]
         [START / SPACE]

GAME:    [状態 / 次の操作] [WAVE] [SCORE / LOCK / LIFE]
         [充電または残り時間 =======================]
         [             静かなゲームフィールド       ]
         [UNDO / CLEAR / CANCEL] [SPACE / EXECUTE]
```

特徴は「プレイヤーが作った一本の線の色と強さが変わる瞬間」です。新たな物語・敵・ルールは追加していません。短い溜め、ヒットストップ、発火速度は従来値のまま、視覚的な落差を作っています。

## 実装境界

- `simulation.js`、`config.js`、`audio.js` は元版から変更なし。衝突半径、経路生成、ロック範囲、発火速度、停止制限時間、Wave、スコア計算、SEと音量を維持。
- `game.js` には描画専用の短命な状態 `artFx` を追加。既存イベントに応じた得点・実行後の経路・局所光のみを保持。reset / retry / 次のSTOP / 被弾で後始末。
- `renderer.js` は盤面、敵の装甲、経路、番号、斬光、余韻を描画。ゲーム状態は読み取り専用。
- `title-art.js` は独立したCanvasのタイトル図。ゲームシミュレーションや入力には触れず、タイトル非表示・タブ非表示時は描画ループを停止。
- `astra.css` は今回のアートディレクションのスタイルを担当。`styles.css` の既存入力レイアウト、練習SVGの同期アニメーションを土台として利用。
- 描画用経路の保持は最大1本、浮遊得点は最大12件。既存パーティクル・残像の上限も維持。
- `prefers-reduced-motion` ではタイトル図を静止し、斬光の局所フラッシュや高速残像を抑える。

## 提示されたコンテスト条件への適合

実行時に読み込むのはローカルのHTML / CSS / Vanilla JavaScriptと既存PNGのみです。Canvas、SVG、Web Audioはブラウザ標準機能です。

外部ライブラリ、jQuery、anime.js、Pixi.js、Three.js、Tone.js、Lucide等のアイコンライブラリ、外部ゲームエンジン、CDN、外部フォントの使用は検出されていません。追加ランタイム依存なし。Bootstrap・Tailwindも使用していません。

Playwrightは開発時のChrome検証ツールで、配信HTMLから読み込まれません。主催者・規約URLは今回指定されていないため、この評価は依頼文に記載された制約との照合です。

## 検証

開始時にNodeロジック57件すべて成功。改装後のChrome結果・確認範囲は [ASTRA_VERIFICATION.md](ASTRA_VERIFICATION.md) に記録します。結果画像はGit管理外の `tests/artifacts/astra/` に保存します。
