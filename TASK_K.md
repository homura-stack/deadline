# TASK K — Gameplay Polish Pass 2

TASK Jの未コミット実装に重ねた調整。Wave 9〜10の視覚的な圧力とクライマックスを維持し、回避・ルート設計に余裕を増やす。数値上のWave 8との大小を合格条件にせず、承認済みの初回調整値を採用した。

## 実装内容

- Near Missは内部座標の中心間40pxまで。被弾判定は従来の14pxのまま。
- 通常回復とNear Missが共通の充電処理を使い、満タン到達時だけ`gaugeReady`イベントを発行。Near Missの実加算値を先に通知する。
- READYの白発光・小さな波動・専用SEはゲージ部分に限定。満タン中は静的に弱く光る。死亡・STOP・退出・リトライで解除し、同一更新内の死亡を優先する。
- TRAININGでは回復倍率を0にした独立ワールドを使用。Near Miss段階を84で開始し、実際の+16で100になる。PCは`SPACE — TIME STOP`、タッチは`TIME STOPをタップ`を表示する。
- 被弾後200msはPicoを白く保持して停止し、その後400msで死亡リアクションを再生。600msでGAME OVER表示または先行入力済みの同Waveリトライを実行する。
- Picoの死亡専用白マスクは既存PNGから実行時にキャッシュする。素材ファイルの生成・変更はない。画像未読込時の代替描画も白くする。
- 保存済みMASTER/SFX値を維持し、共通音量を2.5倍にする。Web Audioのソフトリミッターで重なった音のピークを抑える。通常のSE間の倍率・音階差は維持する。

## パラメータ

| 項目 | TASK J | TASK K |
|---|---:|---:|
| Near Miss半径 | 32px | 40px |
| 通常回復 | 15/sec | 変更なし |
| Near Miss報酬 | +16 | 変更なし |
| 初期 / 最大 / STOPコスト / CANCELコスト | 20 / 100 / 100 / 40 | 変更なし |
| TRAINING Near Miss開始値 | 0、成功後に100へ書換 | 84、実報酬+16で100 |
| 練習ワールドの通常回復倍率 | 1 | 0 |
| READY白発光 / 波動 / 拡張幅 | なし | 120ms / 320ms / 6px |
| READY SE長 / 個別ゲイン | なし | 110ms / 0.04 |
| 被弾ヒットストップ | 100ms | 200ms |
| 白フラッシュ | 120ms | 200ms |
| 死亡リアクション | 被弾から320ms | 停止終了後400ms |
| GAME OVERまで | 420ms | 600ms |
| 共通基準ゲイン | 1 | 2.5（約+8dB） |
| ソフトリミッター開始 / 上限 | なし | 0.8 / 0.95 |
| MASTER / SFX初期設定 | 80 / 90 | 変更なし |

音声接続：各SE → SFX音量 → 共通基準ゲイン → ソフトリミッター → MASTER音量 → 出力。

## Waveごとの変更

射撃間隔倍率は大きいほど低頻度。敵数・タイプ・初期配置・HP・ONE STOP条件は変更していない。

| Wave | 停止時間 秒 | 敵移動速度 | 射撃間隔倍率 | 弾速倍率 |
|---|---:|---:|---:|---:|
| 1 | 5→5 | 18→18 | 1.20→1.20 | .85→.85 |
| 2 | 5→5 | 19→19 | 1.12→1.12 | .88→.88 |
| 3 | 5→6.5 | 20→19 | 1.08→1.10 | .90→.87 |
| 4 | 5→7 | 21→19 | 1.02→1.12 | .92→.88 |
| 5 | 5→7.5 | 22→20 | .98→1.08 | .94→.90 |
| 6 | 5.2→8 | 23→21 | .94→1.06 | .96→.91 |
| 7 | 5.4→8.1 | 21→20 | 1.00→1.10 | .94→.90 |
| 8 | 5.7→9 | 22→21 | .92→1.04 | .96→.92 |
| 9 | 6→10.5 | 23→19 | .86→1.35 | .98→.84 |
| 10 | 6.4→13 | 24→19 | .80→1.55 | 1.00→.86 |

- Wave 4・5：fan 5→4、burst 8→7。
- Wave 6・8：fan 5→4、burst 9→8、rotate 7→6。
- Wave 1〜3・7・9・10の一斉射撃弾数、全Waveのaimは維持。
- 救済は従来どおり失敗3回で1.5倍、5回で2倍を選択可能。新しい基準停止時間に適用し、別Waveへは持ち越さない。

## 今回変更したファイル

TASK Jからの持ち越し差分を含むGit全体の変更一覧とは区別する。

| 区分 | ファイル |
|---|---|
| 本体 | `config.js`, `simulation.js`, `game.js`, `audio.js`, `renderer.js`, `character-assets.js`, `ui-polish.css` |
| 既存テスト更新 | `tests/loop.test.cjs`, `tests/tutorial.test.cjs`, `tests/tutorial-helpers.cjs`, `tests/waves.test.cjs` |
| 回帰テスト追加 | `tests/gauge-ready.test.cjs`, `tests/audio.test.cjs`, `tests/gameplay-k-browser.cjs`, `tests/audio-output-browser.cjs`, `tests/wave-k-browser.cjs` |
| 一括実行 | `tests/run-browser-suite.cjs` |
| 記録 | `TASK_K.md` |

## 検証方法

Node全件は`node --test tests/*.test.cjs`、Chrome全件は`node tests/run-browser-suite.cjs`で実行する。Chrome用には既存のPlaywright環境とローカルサーバーを使用する。ゲーム本体に外部依存は追加しない。

- Node：Near Missの40px境界と14px被弾境界、移動する弾・プレイヤー、READYの順序と再発火、練習ワールドの独立性、Wave設定、救済、音声API・保存設定・音源解放。
- Chrome：実操作でのTRAINING、READYの多重発火防止と同時死亡優先、白い実ピクセル、死亡時間軸、先行リトライ、Reduced Motion、既存の進行・画面・タッチ相当テスト。
- OfflineAudioContext：10種類のイベント音、連続LOCK、10体撃破、Near MissとREADYの重なり、強い同時発音、音量0を実レンダリング。ピーク・RMS・音源解放を検証。
- 通常設定Wave検証：ゲージ回復・停止時間・配置・救済を上書きせず、全10Waveを実操作しENDINGへ進む。テスト終了時にも設定全体の非変更を照合する。
- 結果JSONとスクリーンショットはGit対象外の`tests/artifacts/task-k/`に出力する。既存の描画専用テストには従来の隔離設定を残し、通常難易度の検証には使用しない。

自動経路探索や数値比較は、最終的な体感難易度を保証しない。数値達成だけを理由とする追加調整は実施しない。

## 検証結果（2026-09-08）

- Node：**91 / 91 PASS**（既存82件＋追加9件）。
- Chrome：**22 / 22 PASS**（既存19スクリプト＋追加3スクリプト）。Chrome 152.0.7977.82。一括実行の失敗0件。
- 通常設定・救済なしでWave 1〜10をクリアし、ENDINGへ到達。検証中の設定書換なし。今回の自動操作では各Waveの失敗回数0。
- READYは自然回復・Near Missそれぞれで遷移時だけ発火。同一更新内の被弾では抑制。通常表示・Reduced Motion、先行リトライ、白い実ピクセルと画像未読込時の代替描画がPASS。
- GAME OVER表示は観測上約603〜615ms（描画フレーム間隔を含む）。ヒットストップ中は死亡リアクション・粒子の時間を進めない。
- 音声のLOCK RMSは旧ミックス比**2.500倍**。通常の複合シーケンスのピーク0.371、強い同時発音のピーク0.931で、上限0.95以内。音量0の出力は0、再生後の残存音源0。
- JavaScript構文確認と`git diff --check`：PASS。
- コミット・push・ステージング・素材削除なし。TASK Jの持ち越し差分を保持。
- PCスピーカーでの聞こえ方、主観的な難易度、物理スマートフォンでの操作は人間の確認が必要。

根拠：`tests/artifacts/task-k/suite-logs/summary.json`、`standard-waves.json`、`audio-output.json`、`gameplay.json`。

## 次回人間が確認する5項目

1. Near Missの見た目と判定の一致。
2. READYとTRAININGの因果関係の分かりやすさ。
3. Wave 9〜10の迫力を保ちつつ、回避・ルート設計に余裕が増えたか。
4. 被弾認識とリトライのテンポ。
5. PCスピーカーでの音量・イベント識別と実機タッチでの操作感。
